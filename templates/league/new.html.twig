{% extends 'base.html.twig' %}

{% block title %}New League{% endblock %}

{% block body %}

<div class="col-lg-12">
    <div class="row">
        <div class="col-sm-6 mx-auto">
            <div class="card">
                <div class="card-header">
                  Create New League
                </div>
                <div class="card-body">
                    <h1 class="card-title">Create New League</h1>
                    <p class="card-text">
                    {{ include('league/_form.html.twig') }}
                    </p>
                </div>
                <div class="card-footer text-muted">
                    <a class="btn btn-secondary" href="{{ path('league_index') }}">back to list</a>
                </div>
            </div>
        </div>
    </div>
</div>    

{% endblock %}
