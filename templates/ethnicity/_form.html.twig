{{ form_start(form) }}
    {{ form_row(form.name) }}
    {{ form_row(form.id_faction) }}
    {{ form_row(form.imageFile) }}
    <button class="btn btn-primary">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
