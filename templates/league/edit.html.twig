{% extends 'base.html.twig' %}

{% block title %}Edit League #{{ league.id }}{% endblock %}

{% block body %}
<div class="col-lg-12">
    <div class="row">
        <div class="col-sm-6 mx-auto">
            <div class="card">
                <div class="card-header">
                   League #{{ league.id }}
                   {{ include('league/_delete_form.html.twig') }}
                </div>
                <div class="card-body">
                    <h1 class="card-title">Edit League # {{ league.id }}</h1>
                    <p class="card-text">
                    {{ include('league/_form.html.twig', {'button_label': 'Update'}) }}
                    </p>
                </div>
                <div class="card-footer text-muted">
                    <a class="btn btn-secondary" href="{{ path('league_index') }}">back to list</a>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}