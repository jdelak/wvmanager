{% extends 'base.html.twig' %}

{% block title %}New Ethnicity{% endblock %}

{% block body %}

<div class="col-lg-12">
    <div class="row">
        <div class="col-sm-6 mx-auto">
            <div class="card">
                <div class="card-header">
                  Create new Ethnicity
                </div>
                <div class="card-body">
                    <h1 class="card-title">Create new Ethnicity</h1>
                    <p class="card-text">
                    {{ include('ethnicity/_form.html.twig') }}
                    </p>
                </div>
                <div class="card-footer text-muted">
                    <a class="btn btn-secondary" href="{{ path('ethnicity_index') }}">back to list</a>
                </div>
            </div>
        </div>
    </div>
</div>    

{% endblock %}
